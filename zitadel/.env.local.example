# Zitadel Local Development Environment Variables (Three-Container Setup)
# For use with local Docker Compose setup (Zitadel + PostgreSQL + Cockroach)
# Copy this to .env.zitadel.local and fill in secure values
#
# ⚠️ This is for LOCAL development only, not production
# Production Zitadel is managed by Coolify as a separate service

# ====================================
# Master Key (Encryption)
# ====================================
# MUST be exactly 32 characters (generate: openssl rand -hex 16)
ZITADEL_MASTERKEY=CHANGEME_32_CHAR_HEX_STRING

# ====================================
# Database Configuration
# ====================================
# PostgreSQL admin password (for postgres superuser)
POSTGRES_PASSWORD=CHANGEME_POSTGRES_PASSWORD

# Zitadel database user password (created by init)
ZITADEL_DB_PASSWORD=CHANGEME_ZITADEL_DB_PASSWORD

# ====================================
# First Instance Configuration
# ====================================
# Initial admin user credentials (optional - defaults are used if not set)
# Login format: <username>@<org_name>.<external_domain>
# Example: root@zitadel.auth.vellandi.net
# ZITADEL_FIRSTINSTANCE_ORG_NAME=zitadel
# ZITADEL_FIRSTINSTANCE_ORG_HUMAN_USERNAME=root
# ZITADEL_FIRSTINSTANCE_ORG_HUMAN_PASSWORD=RootPassword1!
