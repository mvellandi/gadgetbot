# ============================================================================
# GadgetBot Production Environment Configuration
# ============================================================================
# Copy this file to .env.production and fill in your values
# Generate secrets: openssl rand -base64 32
# Generate passwords: openssl rand -base64 24

# ============================================================================
# Database Configuration
# ============================================================================
DB_USER=postgres
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ============================================================================
# Application Configuration
# ============================================================================
NODE_ENV=production

# Your production domain (without https://)
APP_DOMAIN=gadgetbot.yourdomain.com
APP_URL=https://gadgetbot.yourdomain.com

# Database connection (use same DB_PASSWORD as above)
DATABASE_URL=postgresql://postgres:CHANGE_ME_STRONG_PASSWORD@postgres:5432/gadgetbot

# Database connection pool settings
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=10

# ============================================================================
# Better Auth Configuration
# ============================================================================
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=CHANGE_ME_32_CHAR_SECRET_HERE

# Same as APP_URL
BETTER_AUTH_URL=https://gadgetbot.yourdomain.com

# ============================================================================
# Zitadel Configuration
# ============================================================================
# Your Zitadel domain (without https://)
ZITADEL_DOMAIN=auth.yourdomain.com

# Master key for Zitadel encryption
# Generate with: openssl rand -base64 32
ZITADEL_MASTERKEY=CHANGE_ME_32_CHAR_SECRET_HERE

# Zitadel issuer URL (same as ZITADEL_DOMAIN with https://)
ZITADEL_ISSUER=https://auth.yourdomain.com

# First-time admin setup (change these!)
ZITADEL_ADMIN_USERNAME=admin
ZITADEL_ADMIN_PASSWORD=CHANGE_ME_STRONG_PASSWORD
ZITADEL_ADMIN_EMAIL=admin@yourdomain.com

# OAuth credentials (get these from Zitadel console after first login)
# Leave blank initially, fill in after Step 7 in deployment guide
ZITADEL_CLIENT_ID=
ZITADEL_CLIENT_SECRET=

# ============================================================================
# Optional: SMTP Configuration (for Zitadel emails)
# ============================================================================
# If you want Zitadel to send emails (password resets, etc.)
# Uncomment and configure these:

# ZITADEL_SMTP_HOST=smtp.gmail.com
# ZITADEL_SMTP_PORT=587
# ZITADEL_SMTP_USER=your-email@gmail.com
# ZITADEL_SMTP_PASSWORD=your-app-password
# ZITADEL_SMTP_FROM=noreply@yourdomain.com
# ZITADEL_SMTP_FROM_NAME=GadgetBot

# ============================================================================
# Security Notes
# ============================================================================
# 1. Never commit this file to Git
# 2. Use strong passwords (minimum 24 characters)
# 3. Use different passwords for each service
# 4. Store a backup of this file securely (password manager)
# 5. Restrict file permissions: chmod 600 .env.production
